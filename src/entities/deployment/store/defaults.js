import { Result } from "common/constant";

const defaults = {
  result: {
    key: Result.SUCCESS,
    desc: ""
  },
  fileList: [],
  list: [],
  pages: [],
  loading: false
};

export default defaults;
